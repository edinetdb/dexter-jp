# Dexter JP v1.1.0-jp — 答え合わせ

自分の仮説を、その会社の有価証券報告書の段落に 1 本ずつ当てて、「裏付ける」「食い違う」「無関係」を出典つきで返します。

```
/check 7203 営業利益が減ったのは為替の影響が主因だと会社は説明している
```

この例だと、トヨタ自動車の FY2026 の有報からこう返ります。

- 「営業利益は減少した」→ **裏付ける**（営業利益 3兆7,662億円、前期比 △1兆293億円）
- 「減少は為替の影響が主因」→ **食い違う**（会社自身の増減要因表で、為替変動 △1,950億円 に対し諸経費 △2兆300億円。うち米国関税 1兆3,800億円）
- 「会社はそう説明している」→ **判定が割れている**（欧州の説明では為替が筆頭、北米では挙がっていない）

`bun run demo` で、この実行をそのまま再生できます。鍵は要りません。

## 新しいもの

`/check <銘柄> <仮説>` — 仮説を主張に分け、有報の対象節（MD&A・事業等のリスク・経営方針）を段落に切り、段落ごとに主張を当てます。段落から確かめられなければ「判定不能」で止めます。モデルに埋めさせません。

判定層 — 型付きの問い（3 択・4 段・真偽）に確率つきで答える層。既定は TypeSafe の Jev。ほかに手元の LLM でラベルだけ代行する経路（`/check` には使いません）と、録画を再生する経路（`bun run demo` とテストだけが使い、環境変数からは選べません）があります。録画に無い問いを聞かれたら**失敗します**。黙って実 API に落ちることはありません。

`bun run demo` — 記録済みの `/check` を再生します。鍵なし・外部通信ゼロ。画面に「録画の再生」と出ます。

`/watch` のデータ層 — ウォッチリストの銘柄に前回以降出た開示を、種別と公表時刻で並べ、開示の性質で 4 群に分けます。どの群も捨てません。コマンドとしての配線は次の版です。

## 外したもの

売買の判断を前提にした既定の振る舞いを 3 つ外しました。`/check` と `/watch` が「売買の指示・目標株価・建玉の大きさを出力しません」と書く以上、同じ製品の他の場所がそれを前提にしていると、書いていることと実態が食い違うためです。いずれも本家から引き継いだものです。

- `src/skills/write-memo/` — long / short の投資メモを書くスキル。説明文が「pitch this stock」「long writeup」で、ヘッダに推奨を書く型でした。既定で発見され、その説明文がシステムプロンプトにも載っていました
- `src/memory/flush.ts` の恒久記憶プロンプト — 「ポートフォリオの判断と配分の変更」「売買履歴と buy/sell の理由」「証券口座の情報」を優先して記憶せよ、と指示していました。何を残すかの指示自体は残し、対象を「何を調べているか・どの開示を使うか・作業上の好み」に差し替えています
- `src/agent/prompts.ts` の memory_search の指示 — 「buy/sell decisions, portfolio suggestions, stock recommendations, or trade sizing を与える前に memory_search を呼べ」でした。memory_search を呼ぶ指示は残し、思い出す対象を差し替えています

## 変わる挙動

**`/check` は `TYPESAFE_API_KEY` が無いと動きません。** 手元の LLM にラベルだけ代行させる構成でも動くようにはしていません。理由は測った結果です。

`/check` の入口には、売買や株価の水準についての助言を求める入力を判定に進めない検査があります。この検査は「決まった語彙と言い回し」と「判定層の票」の 2 つでできています。実装も語彙リストも見せずに別のモデルに書かせた助言入力 30 本で測ると、こうなりました。

| 検査 | 素通りした数 | 正常な 20 本の誤検知 |
|---|---|---|
| 決まった語彙と言い回しだけ | 12/30 | 0/20 |
| 判定層（Jev）の票だけ | 1/30 | 0/20 |
| 2 つを合わせたもの（実装の形） | 0/30 | 0/20 |
| 決まった語彙 + 手元の LLM の票 | 8/30 | 0/20 |

手元の LLM で代行すると 30 本のうち 8 本が素通りします。素通りさせたまま動かすより、止まる方を選びました。鍵なしで動きを見るには `bun run demo` を使ってください。

`bun install` が Chromium を落とさなくなりました（`DEXTER_SKIP_BROWSER=1` のとき）。これまでは無条件に数分・130MB 超のダウンロードが走り、最初の一歩で脱落する原因になっていました。`browser` ツールは不在を検知して入れ直し方を案内します。

秘密のパスが全ツールから読めなくなりました。これまで `.env` や `.dexter/credentials/` の読み取り拒否は `bash` にしか掛かっておらず、`read_file` は作業ディレクトリ配下なら何でも読めました。パスを引数に取る全ツールに広げ、`/check` の記録（`.dexter/checks/`）も対象に加えています。

## 判定層の精度

明確に書き分けた 120 組で、確定した判定の取り違えは 0 件でした（supports 37 / contradicts 40）。判定が割れた組は 4.2%。実際の仮説は曖昧さを含むので、この数字がそのまま出るとは限りません。

測り方: 段落 × 主張の組を 180 作り、会社を分けて調整用 60 と評価用 120 に分割。正解ラベルは 2 者が互いの答えを見ずに付け、食い違った組は選択肢の条文に当てて裁定しました。組の一部で主張が段落の一節を写していたので、そこは言い換えに書き直したうえで正解ラベルを付け直しています。

## この版に入っていないもの

- TradingView 接続。ウォッチリストを読み込む機能は次の版以降に回しました
- Agent SDK モードでの `/check`。分解と要約を SDK の単発呼び出しで回せるかを確かめていないので、この版では未対応です
- `/watch` のコマンド配線。データ層は実装済みで、コマンドとして叩けるようになるのは次の版です
- 数値の主張を財務データで検算する機能。検算の部品はありますが `/check` にまだ配線していないので、この版の `/check` は文章の主張だけを段落に当てます

## 同梱データ

`bun run demo` が再生する段落は有価証券報告書の本文です。同梱データ（`src/data/materials/`）の段落と一字一句同じで（テストで照合しています）、そちらの 1 件ずつに書類管理番号・提出者・書類種別・箇所・取得日・出典表記・編集加工の主体を持たせています。このデータは MIT ライセンスの対象外です（`LICENSE-DATA` を参照）。提出会社からの差し替え・削除の要請に応じる手順も同ファイルにあります。
